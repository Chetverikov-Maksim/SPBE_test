# Проблема с парсингом СПБ Биржи и решения

## ⚠️ ТЕКУЩАЯ ПРОБЛЕМА

Сайт СПБ Биржи (**spbexchange.ru** и **issuers.spbexchange.ru**) **блокирует все автоматические запросы** с ошибкой:

```
HTTP 403 Forbidden
Response: "Access denied"
```

## Что было проверено

### ✗ Попытка 1: Стандартные HTTP запросы
- Результат: 403 Forbidden
- Причина: Сайт детектирует боты

### ✗ Попытка 2: Улучшенные браузерные заголовки
```python
'User-Agent': 'Mozilla/5.0...'
'Accept': 'text/html,application/xhtml+xml...'
'Accept-Language': 'ru-RU,ru;q=0.9...'
'Sec-Fetch-Dest': 'document'
'Sec-Fetch-Mode': 'navigate'
...
```
- Результат: 403 Forbidden
- Причина: Недостаточно для обхода защиты

### ✗ Попытка 3: Cloudscraper (обход Cloudflare)
```python
scraper = cloudscraper.create_scraper()
response = scraper.get(url)
```
- Результат: 403 Forbidden
- Причина: Защита сложнее, чем стандартный Cloudflare

### ✗ Попытка 4: Undetected ChromeDriver
- Результат: Ошибка установки в Docker окружении
- Требуется: Графическая среда для браузера

## Причины блокировки

Сайт использует **продвинутую защиту от ботов**, которая проверяет:
1. **JavaScript выполнение** - требуется реальный браузер
2. **TLS fingerprinting** - уникальная подпись браузера
3. **Поведенческий анализ** - движения мыши, timing, и т.д.
4. **IP репутация** - блокировка датацентров
5. **Browser fingerprinting** - Canvas, WebGL, fonts, и т.д.

## 💡 ВОЗМОЖНЫЕ РЕШЕНИЯ

### Решение 1: Официальное API (РЕКОМЕНДУЕТСЯ) ⭐

**Проверить наличие:**
1. Официального REST/SOAP API от СПБ Биржи
2. FTP/SFTP сервера с данными
3. Excel/CSV выгрузок
4. Доступа для партнеров/брокеров

**Как узнать:**
- Посетить: https://spbexchange.ru/
- Раздел "Для разработчиков" или "API"
- Контакты технической поддержки
- Документация для интеграторов

**Преимущества:**
- ✅ Стабильно
- ✅ Быстро
- ✅ Официально поддерживается
- ✅ Нет блокировок

---

### Решение 2: Selenium + Real Browser

Использовать автоматизацию реального браузера.

**Требования:**
- Машина с графическим интерфейсом (не headless Docker)
- Chrome/Firefox установлен
- Достаточно RAM (>2GB для браузера)

**Установка:**
```bash
pip install selenium webdriver-manager
```

**Пример кода:**
```python
from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager

# Создать драйвер
driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))

# Открыть страницу
driver.get("https://spbexchange.ru/listing/securities/list/")

# Подождать загрузки
import time
time.sleep(3)

# Получить HTML
html = driver.page_source

# Закрыть
driver.quit()
```

**Преимущества:**
- ✅ Работает как настоящий браузер
- ✅ Обходит большинство защит

**Недостатки:**
- ❌ Медленно (2-5 секунд на страницу)
- ❌ Требует графический интерфейс
- ❌ Потребляет много ресурсов

---

### Решение 3: Playwright (альтернатива Selenium)

Более современная альтернатива Selenium.

**Установка:**
```bash
pip install playwright
playwright install chromium
```

**Пример:**
```python
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch()
    page = browser.new_page()
    page.goto("https://spbexchange.ru/listing/securities/list/")

    # Подождать
    page.wait_for_load_state('networkidle')

    # Получить HTML
    html = page.content()

    browser.close()
```

**Преимущества:**
- ✅ Быстрее Selenium
- ✅ Современный API
- ✅ Лучше обходит защиты

**Недостатки:**
- ❌ Тоже требует ресурсов

---

### Решение 4: Обратиться в СПБ Биржу

Написать официальный запрос в техподдержку.

**Контакты СПБ Биржи:**
- Сайт: https://spbexchange.ru/about/contacts/
- Email: info@spbexchange.ru
- Телефон: +7 (812) 325-72-72

**Что указать в письме:**
- Цель: сбор данных по облигациям
- Что нужно: референсные данные, проспекты
- Периодичность: ежедневно
- Запрос доступа к API или альтернативных методов

**Преимущества:**
- ✅ Легальный доступ
- ✅ Техподдержка
- ✅ Документация

---

### Решение 5: Альтернативные источники данных

Использовать другие биржи или агрегаторы.

**Варианты:**
1. **Московская Биржа (MOEX)** - moex.com
   - Есть публичное API
   - Документация: https://www.moex.com/a2193

2. **Cbonds** - cbonds.ru
   - Данные по облигациям
   - Платный доступ

3. **Bloomberg/Reuters**
   - Профессиональные терминалы
   - Дорого

4. **ЦБ РФ**
   - Официальная статистика
   - cbr.ru

---

## 🔧 ТЕКУЩЕЕ СОСТОЯНИЕ ПАРСЕРА

### Что реализовано:
✅ Архитектура парсера (3 модуля)
✅ Конфигурация и маппинг полей
✅ HTTP клиент с retry логикой
✅ **Selenium поддержка для обхода блокировки** ⭐ НОВОЕ!
✅ Next.js JSON extraction для получения данных
✅ Обработка ошибок и логирование
✅ Фильтрация облигаций от акций (через URL параметр securityKind=Облигация)
✅ Пагинация через все страницы
✅ Структура сохранения файлов
✅ CSV экспорт

### Что НЕ работает без Selenium:
❌ Доступ к сайту (403 Forbidden) при использовании requests

### ✅ РЕШЕНИЕ РЕАЛИЗОВАНО!

**Selenium поддержка включена в код!** Чтобы запустить парсер:

1. Установите Chrome или Chromium
2. В файле `spbe_parser/config.py` установите `USE_SELENIUM = True`
3. Запустите парсер обычным образом

Парсер автоматически:
- Скачает ChromeDriver
- Откроет браузер в headless режиме
- Обойдет защиту от ботов
- Извлечет данные из Next.js JSON

**Код полностью готов к работе!**

---

## 📋 РЕКОМЕНДУЕМЫЙ ПЛАН ДЕЙСТВИЙ

### Шаг 1: Проверить официальное API
1. Изучить сайт СПБ Биржи на наличие раздела для разработчиков
2. Проверить документацию
3. Если есть API - использовать его

### Шаг 2: Если API нет - написать в поддержку
1. Составить официальный запрос
2. Указать цель и потребности
3. Запросить рекомендации по доступу к данным

### Шаг 3: Если официальный доступ недоступен
1. Использовать Selenium/Playwright
2. Запускать на локальной машине с GUI
3. Настроить расписание через cron

### Шаг 4: Альтернатива
1. Рассмотреть Московскую Биржу (MOEX API)
2. Или другие источники данных

---

## 🛠 КАК ЗАПУСТИТЬ КОГДА ДОСТУП БУДЕТ ПОЛУЧЕН

```bash
# Установить зависимости
pip install -r requirements.txt

# Для Selenium (если выбрано это решение):
pip install selenium webdriver-manager

# Запустить парсер
python -m spbe_parser.main --all

# Или отдельные модули:
python -m spbe_parser.main --reference-data
python -m spbe_parser.main --prospectus
```

---

## 📞 НУЖНА ПОМОЩЬ?

После выбора решения я помогу:
1. Интегрировать API (если найдется)
2. Адаптировать парсер под Selenium
3. Настроить альтернативный источник
4. Решить любые технические вопросы

**Код готов, нужно только решить проблему доступа к данным!**
